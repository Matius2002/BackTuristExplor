package org.example.proyecturitsexplor.Repositorios;
import org.springframework.stereotype.Service;

@Service
public interface AuditoriaRepositorio {
    // Esta es una interfaz de repositorio vacía que puede ser utilizada para operaciones de auditoría
    // Se espera que se implemente en el futuro para realizar operaciones relacionadas con la auditoría
}

